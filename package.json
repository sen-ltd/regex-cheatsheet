{
  "name": "regex-cheatsheet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interactive regex cheatsheet with live pattern testing.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8080"
  },
  "license": "MIT"
}
